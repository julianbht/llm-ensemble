"""Driving ports (primary/driver ports).

Driving ports are interfaces that the application OFFERS to driving adapters.
These are defined BY the application, implemented BY the application (use cases).
Called BY driving adapters (CLI, Web API, test harness, etc.).
"""
