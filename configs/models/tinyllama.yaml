model_id: tinyllama
provider: ollama
name_hint: tinyllama
context_window: 2048

# Adapter specifications (loaded dynamically)
# Format: module paths are snake_case, class names are UpperCamelCase
provider_module: llm_ensemble.infer.adapters.providers.ollama_adapter
provider_class: OllamaAdapter

# Core inference parameters
temperature: 0.0
max_tokens: 256

# Advanced parameters
additional_params: {}

# Model capabilities
capabilities:
  multilingual: false
