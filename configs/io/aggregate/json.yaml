name_hint: json
description: |
  JSON format for aggregate CLI.
  Reads LLMJudgement records (output from infer CLI).
  Writes AggregatedJudgement records (output from aggregate CLI).

# Adapter specifications (loaded dynamically)
reader_module: llm_ensemble.aggregate.adapters.io.json_judgement_reader
reader_class: JsonJudgementReader
writer_module: llm_ensemble.aggregate.adapters.io.json_aggregated_judgement_writer
writer_class: JsonAggregatedJudgementWriter
