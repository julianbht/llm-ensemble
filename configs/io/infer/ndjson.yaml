name_hint: ndjson
description: |
  NDJSON (newline-delimited JSON) format for infer CLI.
  Reads JudgingExample records (output from ingest CLI).
  Writes ModelJudgement records (output from infer CLI).

# Adapter specifications (loaded dynamically)
# Format: module paths are snake_case, class names are UpperCamelCase
reader_module: llm_ensemble.infer.adapters.io.ndjson_example_reader
reader_class: NdjsonExampleReader
writer_module: llm_ensemble.infer.adapters.io.ndjson_judgement_writer
writer_class: NdjsonJudgementWriter
