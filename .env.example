# ==============================================================================
# LLM Ensemble - Environment Configuration
# ==============================================================================
# This file contains environment variables for secrets and infrastructure only.
# Task parameters (model, dataset, input files) are passed via CLI flags.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your actual keys
#   set -a && source .env && set +a
# ==============================================================================

# API Keys (Required for respective providers)
OPENROUTER_API_KEY=your_openrouter_key_here
HF_TOKEN=your_huggingface_token_here

# Infrastructure (Optional overrides)
OLLAMA_BASE_URL=http://localhost:11434
# HF_ENDPOINT_PHI3_MINI_URL=https://your-endpoint.aws.endpoints.huggingface.cloud
